# Optimized requirements for VICTOR-TTS EXE
# Only essential packages to reduce file size

# Core TTS
edge-tts==6.1.9

# Core ML/Audio
torch>=2.0.0
torchaudio>=2.0.0
numpy>=1.21.0
scipy>=1.7.0
librosa>=0.9.0
soundfile>=0.10.0

# Web Framework
fastapi>=0.68.0
uvicorn>=0.15.0
python-multipart>=0.0.5
aiofiles>=0.7.0

# HTTP/Network
requests>=2.25.0

# Audio Processing
pedalboard>=0.5.0
einops>=0.6.0

# RVC Dependencies (minimal)
noisereduce>=2.0.0
torchcrepe>=0.0.20

# PyInstaller (for building)
pyinstaller>=5.0.0 